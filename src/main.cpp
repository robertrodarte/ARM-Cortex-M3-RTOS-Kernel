/* Used to test zeroed bss and copied data */
static int uninitGlobal;     // Define an uninitialized global variable (.bss)
static int initGlobal = 115; // Define a global variable (.data)

int main()
{
    // Infinite loop
    while (1)
    {
        // Logic goes here
    }

    return 0;
}